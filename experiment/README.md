# Experiments

This directory contains scripts to reproduce the MPI experiment runs and collect results.

## Requirements
- OpenMPI / mpirun installed
- `ngs_mpi` built at:
  `mpi_run/cmake-build-debug/ngs_mpi`

## Run all experiments
```bash
bash experiments/run_all.sh